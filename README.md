# 🌍 Telegram Переводчик

[![Python](https://img.shields.io/badge/Python-3.11+-blue.svg)](https://www.python.org/downloads/)
[![aiogram](https://img.shields.io/badge/aiogram-3.x-green.svg)](https://docs.aiogram.dev/)
[![OpenAI](https://img.shields.io/badge/OpenAI-GPT--4o-orange.svg)](https://openai.com/)
[![License: MIT](https://img.shields.io/badge/License-MIT-yellow.svg)](https://opensource.org/licenses/MIT)

Умный Telegram бот для перевода текста и голосовых сообщений между **6 языками** 🇷🇺🇺🇸🇹🇭🇸🇦🇰🇷🇻🇳 с настраиваемыми пользовательскими предпочтениями и групповыми комнатами для многостороннего общения.

## 🚀 Статус: Готов к продакшену v2.6.1

⚡ **Производительность:** Обработка голоса ~8-9с с early response (было 11.7с), параллельная генерация TTS
🏗️ **Архитектура:** Модульная структура с разделением на core, handlers, services, storage
🧪 **Тестирование:** Офлайн тесты с моками OpenAI, атомарные SQL операции
🧹 **Автоочистка:** Удаление неактивных пользователей и кэша при старте
🏠 **Переговорные комнаты:** Многосторонний перевод для 2-10 участников с автопереводом
🛡️ **Надёжность:** JSON-формат переводов, валидация полноты, fallback для неполных переводов

## ✨ Возможности

### 🔄 Переводы
- **Автоопределение языка** - Русский, английский, тайский, арабский, корейский, вьетнамский
- **Настройки пользователя** - Выбор целевых языков через `/menu`
- **Умная фильтрация** - Переводит только на выбранные языки (не на все)
- **Голосовые сообщения** - Распознавание через Whisper + перевод
- **Голосовые ответы** - TTS с переключением для каждого пользователя
- **Сохранение эмодзи** - Эмодзи корректно передаются через все языки 😊🎉
- **Надёжный парсинг** - JSON-формат ответов от OpenAI вместо текстового парсинга

### 🏠 Переговорные комнаты
- **Создание комнат** - Удобный мастер с выбором названия и языка
- **Присоединение** - Команда `/room join CODE` с выбором своего языка
- **Автоперевод** - Каждый пишет на своём языке, все получают перевод
- **Управление** - Просмотр участников, выход, закрытие комнаты (для создателя)
- **Приглашения** - Простая система шаринга с кодом доступа
- **Поддержка голоса** - Голосовые сообщения работают в комнатах

### ⚡ Производительность и надёжность
- **Early Response** - Транскрипция показывается мгновенно (~3с), перевод в фоне
- **Параллельная обработка** - TTS генерация для всех языков одновременно
- **Умное кэширование** - Переводы (1ч) + TTS (30мин) с постоянным хранением
- **Защита от гонок** - Атомарные операции в базе данных
- **Автоочистка** - Удаление старых пользователей (>3 дней) и кэша при старте
- **Валидация переводов** - Проверка полноты переводов на все запрошенные языки
- **Fallback механизмы** - Отправка оригинального текста если перевод недоступен
- **Детальное логирование** - Отслеживание успешности переводов и отсутствующих языков

### 👑 Админ панель
- **Разделённое меню** - Отдельные разделы для пользователей, сервера и очистки
- **Управление пользователями** - Включение/отключение с сортировкой по активности
- **Мониторинг сервера** - CPU, память, диск, статус службы
- **Очистка данных** - Удаление неактивных пользователей и старого TTS кэша
- **Аналитика** - Счётчики неактивных пользователей, размер кэша

## 🛠️ Установка

```bash
git clone https://github.com/yourusername/tgbot
cd tgbot
python3 -m venv .venv
source .venv/bin/activate  # Linux/Mac
pip install -r requirements.txt
```

## ⚙️ Настройка

1. **Создайте `.env` файл:**
```bash
cp .env.example .env
```

2. **Заполните переменные:**
```bash
TELEGRAM_BOT_TOKEN=your_bot_token
OPENAI_API_KEY=sk-your-openai-key
```

3. **Настройте конфигурацию:**
- `config/development.yaml` - для разработки
- `config/production.yaml` - для продакшена

## 🚀 Запуск

### Разработка
```bash
source .venv/bin/activate
python -m src.main
```

### Продакшен (systemd)
```bash
sudo cp translator-bot.service /etc/systemd/system/
sudo systemctl daemon-reload
sudo systemctl enable translator-bot
sudo systemctl start translator-bot
```

## 🧪 Тестирование

```bash
# Запуск всех тестов
python -m pytest tests/

# Тесты базы данных
python -m pytest tests/test_database.py -v

# Тесты определения языка
python -m pytest tests/test_language_detection.py -v
```

## 📁 Структура проекта

```
tgbot/
├── src/
│   ├── core/          # Основные компоненты (app, config, cache)
│   ├── handlers/      # Обработчики сообщений
│   ├── services/      # Бизнес-логика (перевод, голос, аналитика)
│   └── storage/       # База данных и хранение
├── config/            # YAML конфигурации
├── tests/            # Тесты с моками
└── data/             # SQLite база данных
```

## 🔧 Системные требования

- **Python:** 3.11+ (совместим с 3.13)
- **FFmpeg:** Для обработки аудио
- **SQLite:** Встроенная база данных
- **Память:** ~200MB RAM
- **Диск:** ~50MB для логов и кэша

## 📝 Команды бота

- `/start` - Начать работу с ботом
- `/menu` - Настройки языков перевода
- `/room` - Создать или присоединиться к переговорной комнате
- `/room join CODE` - Присоединиться к комнате по коду
- `/admin` - Админ панель (только для админов)
- `/help` - Справка по командам

## 🐛 Решение проблем

### Python 3.13 + pydub
Если возникают проблемы с модулем `audioop`, бот автоматически переключается на ffmpeg fallback.

### Неполные переводы
**v2.6.1** исправляет проблемы с неполными переводами:
- JSON-формат вместо текстового парсинга
- Валидация полноты переводов
- Fallback для отсутствующих языков
- Корректная обработка эмодзи

### Нехватка места на диске
```bash
# Очистка логов
sudo journalctl --vacuum-time=7d
rm -rf logs/archive/*
```

### Проблемы с FFmpeg
```bash
# Ubuntu/Debian
sudo apt update && sudo apt install ffmpeg

# Проверка
ffmpeg -version
```

## 📄 Лицензия

MIT License - см. файл [LICENSE](LICENSE)

## 🤝 Поддержка

При возникновении вопросов создавайте Issues в GitHub репозитории.
